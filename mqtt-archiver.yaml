mqtt:
  broker: tcp://localhost:1883
  username: dev
  password: secret
  timeout: 5s
  gracePeriod: 2s
workingDir: .podman-compose/mqtt-archiver/
exclude: ^test
s3:
  endpoint: localhost:9000
  accessKey: dev
  secretKey: secret123
  ssl: false
  bucket: mqtt
